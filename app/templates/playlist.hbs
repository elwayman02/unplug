<table>
    <tr>
        <th>Artist</th>
        <th>Title</th>
    </tr>
    {{#each model as |track|}}
        <tr>
            <td>{{track.author}}</td>
            <td>{{track.title}}</td>
        </tr>
    {{/each}}
</table>