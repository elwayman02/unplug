<p>
    {{#link-to 'me.playlist.raw'}}
        View Raw Playlist
    {{/link-to}}
    to copy it for your own files!


</p>
<div class="playlist">
    <div class="playlist-header">
        <span>Track Image</span>
        <span {{action 'sortByArtist'}}>Artist</span>
        <span {{action 'sortByTitle'}}>Title</span>
        <span>Link</span>
    </div>
    {{#each tracks as |track|}}
        <div class="playlist-row">
            <span class="track-img"><img src="{{track.image}}"/></span>
            <span>{{track.author}}</span>
            <span>
                {{#if track.url}}
                    <a href="{{track.url}}" target="_blank">{{track.title}}</a>
                {{else}}
                    {{track.title}}
                {{/if}}
            </span>
            <span>
                {{#if (eq track.format 1)}}
                    {{#link-to 'youtube' track.cid}}<button>Listen</button>{{/link-to}}
                {{else}}
                    {{#link-to 'soundcloud' track.cid}}<button>Listen</button>{{/link-to}}
                {{/if}}
            </span>
        </div>
    {{/each}}
</div>